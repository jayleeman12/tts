import React, { useEffect } from 'react';
import { View } from 'react-native';

export type ChapterProps = {
    pages: string[];
}

const Chapter: React.FunctionComponent<ChapterProps> = props => {
    return (
        <View>
        </View>
    );
}

export default Chapter;