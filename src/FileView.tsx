import React from 'react';
import { Text, View } from 'react-native';

const FileView = () => (
    <View>
        <Text>
            file view
        </Text>
    </View>
);

export default FileView;